#include <math.h>
#include "stdafx.h" 
#include <stdio.h> 
#include <conio.h>
#include <iostream> 
int  main() 
{
    double x, y, f;
     x = 30.5;
    cout << "Введите переменную y"; 
    cin >> y ; 
    f = sqrt|(x) + pow(X,Y);
    cout<<"F="<<F;
    return 0;
}
